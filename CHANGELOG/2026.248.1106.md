# 2026.248.1106 — migrate: harvest legacy record: deploy field into instrument: (Cutover A)

## Summary

Cutover A migration entry: the legacy deploy `record:` field (the G5 bed-level whole-run recording wrap) is HARVESTED into the new `instrument:` capture model — version-anchored `2026.248.1030` (`record-field-to-instrument` in migrations.cue): `record_name` becomes the instrument id (default `recording`), the remaining `#RecordWrap` value fields pass through as the `record: {method: session, ...}` verb input; a `record:` carrying `method:` is PLAN-STEP verb sugar (a live probe word), never the deploy field — structurally distinguishable by value shape alone, so step sugar is NEVER touched.

`reshape_record_field.go` walks the whole document tree (a deploy `record:` can sit on any substrate node).

## How tested (fresh, live)

```
$ cd plugin-migrate/candy/plugin-migrate && go test -count=1 ./...
ok  github.com/opencharly/plugin-migrate/candy/plugin-migrate  0.006s
```

**Live migration run** (dev binary built from this tree + the merged spec/sdk tags; legacy 2026.240.1943-stamped config with a `record:` field):

```
$ charly migrate
applied record-field-to-instrument (schema 2026.248.1030)
migrated to schema 2026.248.1030

# migrated config (the record: block harvested):
mig-proof-bed:
    pod:
        image: web
        disposable: true
        plan:
            - check: the subject step
              id: subject
              command: "true"
        instrument:
            - id: pr-flow
              phase: [live]
              record:
                method: session
                fps: 4
                artifact: /tmp/pr-flow.cast
```

**Version anchoring (finding 3):** `#SchemaVersion` on spec main IS `2026.248.1030` (verified from `spec main:schema/version.cue`). The `go.mod` bump to `v0.2026248.1043` is the spec repo's TAG number (tag-on-merge CalVer) — a different numbering domain from `#SchemaVersion`; the engine validates entries within `[SchemaFloor, SchemaHead]` and this entry targets the head. The stale "must be re-pinned" comment is corrected in this push.

## Rulebook compliance

- **R5**: the legacy field is harvested, never dual-kept; the companion plugin-check PR deletes the wrap machinery in the same wave.
- **R3**: one reshape implementation, shared by the engine harness.
- **R6 / git-workflow**: PR-only; fresh branch; no force-push.
- **Cutover law**: version-anchored to the spec#96 `#SchemaVersion` bump, same wave as spec/sdk/plugin-check.

## Change classification

- **Class**: code change (migration table + reshape; wire-key surface is the merged spec#96).
- **Verification gate**: fresh `go test -count=1` + the LIVE migration run above (the changed runtime path — `recordFieldToInstrument` — executed live on a legacy-stamped config). `task verify` + the R10 bed roster run at the wave's charly PR.
- **Attribution**: analysed on a live system (fresh uncached tests + live migration of the changed path; full umbrella gate deferred to the wave's charly PR per the plan).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (analysed on a live system)*